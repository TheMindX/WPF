<UserControl x:Class="TimeSheet.doubleTextBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="37" d:DesignWidth="316">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <TextBox x:Name="m_value" Margin="5,0" TextWrapping="Wrap" Text="1" Background="Black" Foreground="White" Height="22" BorderThickness="1" Grid.ColumnSpan="2" FontSize="16" AcceptsReturn="False" PreviewTextInput="textInput"/>
    </Grid>
</UserControl>
